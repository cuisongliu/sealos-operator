apiVersion: apps.github.com/v1beta1
kind: Application
metadata:
  labels:
    app.kubernetes.io/name: sealos-operator
    app.kubernetes.io/managed-by: kustomize
  name: application-sample
spec:
  # TODO(user): Add fields here
